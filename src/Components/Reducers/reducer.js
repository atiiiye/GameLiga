const initialState = { historyProp: '' }

function reducer(state = initialState, action) {

    // switch (action.type) {
    //     case 'HistorySlider':
    //         return {}
    // }

}

export default reducer;
